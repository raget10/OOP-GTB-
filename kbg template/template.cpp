#include "template.h"

template <typename T>
void Rock<T>::Attack(Inventory<T>* selfInventory, Inventory<T>* opponentInventory) {
    // Implementasi serangan Rock di sini
}

template <typename T>
void Paper<T>::Attack(Inventory<T>* selfInventory, Inventory<T>* opponentInventory) {
    // Implementasi serangan Paper di sini
}

template <typename T>
void Scissor<T>::Attack(Inventory<T>* selfInventory, Inventory<T>* opponentInventory) {
    // Implementasi serangan Scissor di sini
}

template <typename T>
void Protection<T>::Attack(Inventory<T>* selfInventory, Inventory<T>* opponentInventory) {
    // Implementasi serangan Protection di sini
}
